using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RunSim : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Turn off all buttons
// GameObject.Find('center-button').SetActive(true);
// GameObject.Find('top-right-button').SetActive(false);
// GameObject.Find('bottom-right-button').SetActive(false);
// GameObject.Find('top-left-button').SetActive(false);
// GameObject.Find('bottom-left-button').SetActive(false);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
